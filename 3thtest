#include "pch.h"

int test1(int s, int x)
{
 if (s != 0)
 {
  switch (x)

  {

  case 1: {return 1; break; }

  case 2: {return 2; break; }

  case 3: {return 3; break; }

  case 4: {return 4; break; }

  default: {"输出有误！"; }

  }
 }
}

TEST(TestCaseName, TestName) {
 EXPECT_EQ(test1(1, 2), 2);
 EXPECT_EQ(test1(2, 3), 3);

}
